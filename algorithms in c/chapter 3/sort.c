#include<stdlib.h>
#include<stdio.h>

typedef struct node* link;
struct node
{
    int item;
    link next;
};

int main()
{
            struct node heada, headb;
            link t, u, x, a = &heada, b;
            for(i = 0, t = a; i<N; i++)
            {
                t->next = malloc()
            }
}
